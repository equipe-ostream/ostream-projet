{% extends 'baseSecurityDashboard.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    <h1>Créez votre compte :</h1>
    <div class="col-md-6">
        {{ form_start(form, { attr: { class: 'login-form' } }) }}
        <div class="col-md-12">
            <div class="input-field col s12">
                <i class="material-icons prefix pt-5 yellowcrypt"></i>
                {{ form_errors(form.nom) }}
                {{ form_widget(form.nom) }}
            </div>
        </div>
        <div class="col-md-12">
            <div class="input-field col s12">
                <i class="material-icons prefix pt-5 yellowcrypt"></i>
                {{ form_errors(form.email) }}
                {{ form_widget(form.email) }}
            </div>
        </div>
        <div class="col-md-12">
            <div class="input-field col s12">
                <i class="material-icons prefix pt-5 yellowcrypt"></i>
                <span class="red-text">{{ form_errors(form.password.first) }} </span>
                {{ form_widget(form.password.first) }}
            </div>
        </div>
        <div class="col-md-12">
            <div class="input-field col s12">
                <i class="material-icons prefix pt-5 yellowcrypt"></i>
                {{ form_widget(form.password.second) }}
            </div>
        </div>
        <div class="row">
            <div class="input-field col s12 mb-5">
                {{ form_widget(form.submit) }}
            </div>
        </div>
        {{ form_end(form) }}
    </div>
    <br/>
    <div>
        <a href="{{ path('login') }}">Déjà inscrit ? Identifiez-vous </a>
    </div>



{% endblock %}